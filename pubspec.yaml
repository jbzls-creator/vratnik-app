name: vratnik_app
description: "Flutter app – gatekeeper / master-guest request system"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  provider: ^6.0.5

  # 🔥 FIREBASE
  firebase_core: ^2.32.0
  cloud_firestore: ^4.17.5

  # 🔥 LOCAL STORAGE (HOST DATA)
  shared_preferences: ^2.2.2

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
